import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dopamindasher.app',
  appName: 'Dopamine Dasher',
  webDir: 'dist/public'
};

export default config;
